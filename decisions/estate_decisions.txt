country_decisions = {

	estate_picker_decision = {
		color = { 182 50 218 }
		potential = {
			OR = {
				OR = {
					num_of_owned_provinces_with = {
						trade_goods = gu_coal
						value = 5
					}

					capital_scope = {
						OR = {
							region = dolnoslaskie_region
							region = opolskie_region
							region = slaskie_region
						}
					}
				}
				AND = {
					innovativeness = 50
					NOT = { 
						religion_group = communism_group
					}
				}
				OR = {
					colony = 1
					gu_finances = 1
					NOT = { 
						OR = {
							current_age = age_of_discovery
							current_age = age_of_reformation
						}
						religion_group = communism_group
					}
				}
				AND = {
					OR = {
						num_of_owned_provinces_with = {
							value = 5
							has_terrain = farmlands
						}
						num_of_owned_provinces_with = {
							value = 10
							has_terrain = grasslands
						}
					   
					   capital_scope = {
							OR = {
								region = lubelskie_region
								region = wielkopolska_region
								region = kujawsko_pomorskie_region
							}
						}
					}
					NOT = { 
						religion_group = communism_group
					}
				}
			}
		}

		allow = {
			custom_trigger_tooltip = {
				tooltip = estate_picker_decision_tooltip
				always = yes
			}
		}
		
		effect = {
			custom_tooltip = estate_picker_decision_effect_tooltip
			
			hidden_effect = { country_event = { id = gmina_estates.2 } }
		}

		ai_will_do = {
			factor = 0
		}
	}

	estate_possible_decision = {
		color = { 182 50 218 }
		potential = {
			always = yes
		}

		allow = {
			custom_trigger_tooltip = {
				tooltip = estate_possible_decision_tooltip
				always = yes
			}
		}
		
		effect = {
			custom_tooltip = estate_possible_decision_effect_tooltip
			hidden_effect = { country_event = { id = gmina_estates.3 } }
		}

		ai_will_do = {
			factor = 0
		}
	}

}