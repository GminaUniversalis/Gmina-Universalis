namespace = gmina_common_flavor

country_event = {
	id = gmina_common_flavor.1
	title = gmina_common_flavor.1.t
	desc = gmina_common_flavor.1.d
	picture = ROYAL_STRESS_eventPicture

	is_triggered_only = yes
	
	trigger = {
		ai = no

		ROOT = {
			any_owned_province = {
				OR = {
					has_terrain = forest
					has_terrain = woods
				}
			}
		}
	}
	option = {
		name = gmina_common_flavor.1.a
		custom_tooltip = gmina_common_flavor.1.tt
		add_manpower = -0.001

	}

	immediate = {

		hidden_effect = {
			change_variable = {
				which = boar_counter
				value = 1

			}
		}
		
	}
}

country_event = {
	id = gmina_common_flavor.2
	title = gmina_common_flavor.2.t
	desc = gmina_common_flavor.2.d
	picture = SMOLENSK_eventPicture
	
	fire_only_once = yes
	major = yes

	major_trigger = {
		religion = conservatism_PIS
	}

	trigger = {
		
		1500 = {

			check_variable = {
				which = clock_year
				value = 2010
			}

		}
		1500 = {
			check_variable = {
				which = clock_month
				value = 4
			}
		}

	}

	mean_time_to_happen = {
		days = 1
	}

	option = {
		name = gmina_common_flavor.2.a

		hidden_effect = {
			set_global_flag = bombing_plot_delusions
		}
		tooltip = {
			add_country_modifier = {
				name = smolensk_catastrophe
				duration = 365
			}
		}
		add_stability = -2

		custom_tooltip = this_will_have_consequences
	}
	option = {
		name = gmina_common_flavor.2.b

		hidden_effect = {
			set_global_flag = russian_intervention_delusions
		}
		tooltip = {
			add_country_modifier = {
				name = smolensk_catastrophe
				duration = 365
			}
		}
		add_stability = -2

		custom_tooltip = this_will_have_consequences
	}
	option = {
		name = gmina_common_flavor.2.c

		hidden_effect = {
			set_global_flag = controller_sabotage_delusions
		}
		tooltip = {
			add_country_modifier = {
				name = smolensk_catastrophe
				duration = 365
			}
		}
		add_stability = -2

		custom_tooltip = this_will_have_consequences
	}
	option = {
		name = gmina_common_flavor.2.dd

		hidden_effect = {
			set_global_flag = technical_sabotage_delusions
		}
		tooltip = {
			add_country_modifier = {
				name = smolensk_catastrophe
				duration = 365
			}
		}
		add_stability = -2

		custom_tooltip = this_will_have_consequences
	}
	option = {
		name = gmina_common_flavor.2.e

		hidden_effect = {
			set_global_flag = polish_russian_conspiracy_delusions
		}
		tooltip = {
			add_country_modifier = {
				name = smolensk_catastrophe
				duration = 365
			}
		}
		add_stability = -2

		custom_tooltip = this_will_have_consequences
	}
	immediate = {
		set_global_flag = smolensk_disaster_enable

		hidden_effect = {

			add_country_modifier = {
				name = smolensk_catastrophe
				duration = 365
			}
			
		}

	}
}